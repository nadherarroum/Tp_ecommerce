@model IEnumerable<TP_Ecommerce.Models.Categorie>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NameCat)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NameCat)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDCat }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDCat }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDCat })
        </td>
    </tr>
}

</table>
